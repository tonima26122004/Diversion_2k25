/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        libra: ["Averia Serif Libre"], // Custom font family
      },
      keyframes: {
        shine: {
          '0%': { 'background-position': '100%' },
          '100%': { 'background-position': '-100%' },
        },
      },
      animation: {
        shine: 'shine 3s linear infinite', // Define the shine animation
      },
    },
  },
  plugins: [require("tailwind-scrollbar")], // Optional: Add scrollbar plugin
};